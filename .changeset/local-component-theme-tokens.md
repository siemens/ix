---
'@siemens/ix': patch
---

Move component token defaults from the global theme output into dedicated component `*.vars.scss` files with `--ix-*` custom properties and classic light/dark fallback values that follow the active color scheme.
