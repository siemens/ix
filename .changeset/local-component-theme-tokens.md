---
'@siemens/ix': patch
---

Move component token defaults from the global theme output into dedicated component `*.vars.scss` files with `--ix-*` custom properties and classic dark fallback values.
