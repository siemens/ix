---
'@siemens/ix': patch
---

Update component theming so local `--ix-*` variables and shared input styles use the latest theme and system tokens directly, without bundling fallback values in component theme references.
