---
'@siemens/ix': patch
---

Update component theming so local `--ix-*` variables use their owning component namespace and resolve the latest theme and system tokens directly, without bundling fallback values in component theme references.
