---
'@siemens/ix': patch
---

Update component theming so local `--ix-*` variables use the latest theme and system tokens directly, with synchronized Classic Light and Dark fallbacks when theme CSS is unavailable.
