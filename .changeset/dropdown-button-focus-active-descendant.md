---
'@siemens/ix': patch
---

**ix-dropdown-button:** Hide the trigger's focus outline while the dropdown is expanded so focus is shown only on the active dropdown item.

**ix-breadcrumb:** Stop clipping the focus outline of items flush with the host edges (e.g. the first breadcrumb item or the `...` previous-items button).
