---
'@siemens/ix': minor
---

Make `ix-group` keyboard- and screen-reader-accessible with separate header select and expand controls, disclosure ARIA (`aria-expanded` / `aria-controls`), Space/Enter, Escape to collapse, and optional `i18nExpandGroup` / `i18nCollapseGroup` labels.
