---
'@siemens/ix': minor
---

Make `ix-group` keyboard- and screen-reader-accessible with separate header select and expand controls, disclosure ARIA (`aria-expanded` / `aria-controls`), Space/Enter, Escape to collapse, accessible names from the header text (expanded state from `aria-expanded` only), screen-reader-compatible context-menu keyboard navigation, and `aria-pressed` on selectable `ix-group-item` entries.
