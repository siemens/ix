---
"@siemens/ix-aggrid": major
---

`ix-aggrid` now requires applications to install `@siemens/ix` directly. Add `@siemens/ix` to your app dependencies when using the AG Grid theme so the required IX styles and assets stay available.
