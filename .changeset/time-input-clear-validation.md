---
'@siemens/ix': minor
---

Add a `clear()` method to `ix-time-input` to reset its value and validation state.

Also align `ix-time-input` validation behavior for required fields, `novalidate` forms, and dynamic `required` toggling.
