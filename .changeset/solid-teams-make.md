---
'@siemens/ix': patch
---

Make sure property `textareaRows` is applied when set on **ix-textarea**.
Properties `textareaHeight` and `textareaWidth` handle units like REM, EM, px, and % correctly.
