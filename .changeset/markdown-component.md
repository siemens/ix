---
'@siemens/ix': minor
---

Add the `ix-markdown` component with iX styling, task-list checkboxes, registered web components, runtime data bindings, and a required renderer function. Import the bundled Comark renderer from `@siemens/ix/markdown`, or opt into explicit-language Highlight.js syntax highlighting with `@siemens/ix/markdown/highlight`.
