---
'@siemens/ix': patch
---

Restrict application-switch destinations to relative URLs and the `http`, `https`, `mailto`, and `tel` protocols, and prevent opened applications from accessing `window.opener`.
