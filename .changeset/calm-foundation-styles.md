---
'@siemens/ix': minor
---

Add foundation and legacy utility CSS entry points so consumers can migrate toward loading only IX theme/foundation styles while keeping existing global CSS imports available.
