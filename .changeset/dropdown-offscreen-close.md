---
'@siemens/ix': patch
---

**ix-dropdown:** close when the anchor leaves the visible region (viewport by default, or optional **`intersectionRoot`** for a scroll container), after a short debounce.

**ix-select:** **`intersectionRoot`** prop, passed through to the internal dropdown.
