---
'@siemens/ix': patch
---

Document JSDoc and Markdown conventions for API reference text (bold with `**`, inline code with backticks, periods / `@deprecated` / `@since` / lists, and avoiding nested bold+code that breaks the Markdown renderer) in **CONTRIBUTING.md**. TypeDoc-based util API MDX preserves inline code spans in summaries via the documentation generator.
