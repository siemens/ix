---
'@siemens/ix': minor
---

Add icon size `20` on **ix-icon-button** and **ix-icon-toggle-button** (32×32 control, 20px glyph). Omitted `size` now defaults to `20`. Leading and trailing icons on **ix-button** use size `20`.
