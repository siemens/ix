---
'@siemens/ix': minor
---

Add icon size `20` on **ix-icon-button** and **ix-icon-toggle-button** (32×32 control, 20px glyph). Omitted `size` now defaults to `20`. Leading and trailing icons on **ix-button** use size `20`. Labeled **ix-dropdown-button** leading icons use size `20`; the chevron stays `24`. Icon-only dropdown buttons use the 20px icon-button default. **ix-dropdown-item** leading icons and submenu chevrons use size `20`; the checkmark stays `16`. **ix-select** chevron uses the 20px icon-button default; the clear button stays `16`. Date, time, and datetime field calendar/clock icons stay `16`. **ix-date-picker** month arrows use the 20px icon-button default. **ix-chip** leading icons use size `20`; the close icon stays `16`. **ix-pill** leading icons stay `16`. **ix-badge** label icons stay `16`; status icons use size `20`. **ix-blind** collapse chevron and leading header icons use size `20`. **ix-message-bar** status icons and the dismiss button use size `20`. **ix-event-list-item** chevrons use size `20`. **ix-breadcrumb-item** leading icons use size `20`; the chevron stays `16`. **ix-card-list** header chevrons use the 20px icon-button default; the overflow more-cards icon stays `32`. **ix-group** expand chevrons use size `20`; the context-menu button uses the 20px icon-button default; **ix-group-item** leading icons stay `16`. **ix-key-value** leading icons use size `20`. **ix-pane** collapse/close and heading icons use size `20`. **ix-tree-item** expand chevrons use size `20`. **ix-upload** status icons use size `20`. **ix-workflow-step** step icons use size `20`. **ix-checkbox** and **ix-radio** controls use size `20` (24×24 hit area; the checkbox check stays `18`; the radio inner dot stays `8`). **ix-avatar** stays `32`. **ix-tooltip** title icons stay `16`. **ix-link-button** icons stay `16`. **ix-pagination** prev/next buttons use the 20px icon-button default. **ix-flip-tile** flip buttons use the 20px icon-button default. **ix-popover-header** title and close icons use size `20`. **ix-toast** status icons stay `24`; the close button uses the 20px icon-button default. **ix-chat-input** send uses the 20px icon-button default. **ix-application-header** app-switch and overflow stay `24`. **ix-content-header** back stays `24`. **ix-menu-item** leading icons stay `24`; **ix-menu-category** chevron stays `24`. Menu expand (lg/md) and the small-viewport app-switch stay `24`. **ix-menu-about**, **ix-menu-settings**, and **ix-menu-about-news** close buttons use the 20px icon-button default. **ix-tab-item** labeled and icon-only icons stay `16`; rounded tabs stay `32`. **ix-modal-header** title icons stay `32`; the close button uses the 20px icon-button default.
