# Breaking Changes V6

This document lists breaking changes introduced in Siemens Industrial Experience V6.


## Contents

- [Stencil collection optional peers](#stencil-collection-optional-peers)
- [Siemens design tokens](#siemens-design-tokens)
- [ECharts computed CSS properties](#echarts-computed-css-properties)
- [Generated component token aliases](#generated-component-token-aliases)
- [Global CSS and Sass entry points](#global-css-and-sass-entry-points)
- [Removed legacy Sass modules](#removed-legacy-sass-modules)
- [Removed Sass mixins](#removed-sass-mixins)
- [Icon and component color properties](#icon-and-component-color-properties)
- [Content header text wraps by default](#content-header-text-wraps-by-default)
- [Hydration class changed to attribute](#hydration-class-changed-to-attribute)

## Stencil collection optional peers

The Stencil collection has four newly optional peer dependencies:
`@floating-ui/dom`, `animejs`, `luxon`, and `@stencil/core`.
`@siemens/ix-icons` remains a required peer. Collection metadata and output
consumers must install all five packages:

```sh
pnpm add @floating-ui/dom @siemens/ix-icons @stencil/core animejs luxon
```

Use the equivalent command for npm, pnpm, or another package manager.
`@siemens/ix-icons` remains required for all `@siemens/ix` consumers. Root,
custom-elements, loader, hydrate, and framework consumers do not need to install
the other four optional peers solely for IX because those outputs bundle the
runtime libraries.

## Siemens design tokens

IX components now use SI Theme 6 system and reference tokens instead of legacy
IX `--theme-*` tokens. This is a semantic migration, not a prefix removal:
select a purpose-specific background, border, or text token for each use.
Semantic color tokens use the `--si-sys-color-*` namespace.

The mappings below use code-facing CSS custom-property names. Source hierarchy
paths have been flattened to the legacy `--theme-*` and new `--si-sys-*` syntax;
trailing `*` markers in the source mapping are metadata and are omitted here.
The existing typography and shadow examples map as follows:
`--theme-body` -> `--si-sys-typography-body` and `--theme-shadow-4` ->
`--si-sys-color-effects-shadow-4`.

Replace legacy token references according to how the value is used. For
example:

```css
/* Before */
color: var(--theme-color-std-text);

/* After */
color: var(--si-sys-color-text-primary);
```

Sass code can use the new public system-token module:

```scss
@use '@siemens/ix/scss/tokens/system' as sys;

color: #{sys.$si-sys-color-text-primary};
```

### Token value shapes

Replacement tokens are not guaranteed to have the same CSS value type as the
legacy token. `--si-sys-color-effects-shadow-*` tokens contain a complete
`box-shadow` value, including offsets, blur/spread where applicable, and color.
Assign a complete shadow token directly to `box-shadow`. Do not mechanically
substitute one where a legacy `--theme-color-shadow-*` token supplied only the
color component of a longer shadow declaration; these color tokens have no
direct replacement and require manual reconstruction or review. Likewise,
`--si-sys-typography-*` tokens contain complete `font` shorthand values, so use
them with `font`, not as a `font-family` or size fragment.

```css
/* A complete shadow token can be replaced directly. */
/* Before */
box-shadow: var(--theme-shadow-4);

/* After */
box-shadow: var(--si-sys-color-effects-shadow-4);

/* Do not mechanically replace a legacy color component this way. */
/* Before */
box-shadow: 0 2px 4px var(--theme-color-shadow-1);

/* Incorrect */
box-shadow: 0 2px 4px var(--si-sys-color-effects-shadow-1);
```

### Special replacements

These two legacy tokens resolve to the CSS keyword `transparent`, not to a
system-token variable. They are also listed as investigate entries in the
source guide, but are considered resolved here and are not repeated in the
manual-review table.

| Legacy token          | Replacement   |
| --------------------- | ------------- |
| `--theme-color-0`     | `transparent` |
| `--theme-color-ghost` | `transparent` |

### Direct mappings

Apply these replacements wherever the legacy token is used.

#### Background and interaction

| Legacy token                               | Replacement                                         |
| ------------------------------------------ | --------------------------------------------------- |
| `--theme-color-1`                          | `--si-sys-color-background-0`                       |
| `--theme-color-2`                          | `--si-sys-color-background-1`                       |
| `--theme-color-ghost--hover`               | `--si-sys-color-background-hover`                   |
| `--theme-color-ghost--active`              | `--si-sys-color-background-selected`                |
| `--theme-color-ghost--selected`            | `--si-sys-color-background-selected`                |
| `--theme-color-ghost--selected-hover`      | `--si-sys-color-background-hover`                   |
| `--theme-color-ghost--selected-active`     | `--si-sys-color-background-selected`                |
| `--theme-color-ghost-alt`                  | `--si-sys-color-background-1`                       |
| `--theme-color-ghost-alt--hover`           | `--si-sys-color-background-hover`                   |
| `--theme-color-ghost-alt--active`          | `--si-sys-color-background-selected`                |
| `--theme-color-ghost-alt--selected`        | `--si-sys-color-background-selected`                |
| `--theme-color-ghost-alt--selected-hover`  | `--si-sys-color-background-hover`                   |
| `--theme-color-ghost-alt--selected-active` | `--si-sys-color-background-selected`                |
| `--theme-color-primary--contrast`          | `--si-sys-color-text-on-accent`                     |
| `--theme-color-secondary`                  | `--si-sys-color-background-accent-secondary`        |
| `--theme-color-secondary--hover`           | `--si-sys-color-background-accent-secondary-hover`  |
| `--theme-color-secondary--active`          | `--si-sys-color-background-accent-secondary-active` |
| `--theme-color-component-1`                | `--si-sys-color-background-1`                       |
| `--theme-color-component-1--hover`         | `--si-sys-color-background-hover`                   |
| `--theme-color-component-1--active`        | `--si-sys-color-background-selected`                |
| `--theme-color-component-7--hover`         | `--si-sys-color-background-accent-secondary-hover`  |
| `--theme-color-component-7--active`        | `--si-sys-color-background-accent-secondary-active` |
| `--theme-color-component-error`            | `--si-sys-color-background-danger-subtle`           |
| `--theme-color-component-info`             | `--si-sys-color-background-information-subtle`      |
| `--theme-color-1--hover`                   | `--si-sys-color-background-hover`                   |
| `--theme-color-1--active`                  | `--si-sys-color-background-selected`                |
| `--theme-color-ghost-primary--hover`       | `--si-sys-color-background-accent-secondary-hover`  |
| `--theme-color-ghost-primary--active`      | `--si-sys-color-background-accent-secondary-active` |
| `--theme-color-component-warning`          | `--si-sys-color-background-warning-subtle`          |

#### Status

| Legacy token                       | Replacement                                    |
| ---------------------------------- | ---------------------------------------------- |
| `--theme-color-alarm--hover`       | `--si-sys-color-background-danger-hover`       |
| `--theme-color-alarm--active`      | `--si-sys-color-background-danger-active`      |
| `--theme-color-alarm--contrast`    | `--si-sys-color-text-on-danger`                |
| `--theme-color-success--hover`     | `--si-sys-color-background-success-hover`      |
| `--theme-color-success--active`    | `--si-sys-color-background-success-active`     |
| `--theme-color-success--contrast`  | `--si-sys-color-text-on-success`               |
| `--theme-color-info--hover`        | `--si-sys-color-background-information-hover`  |
| `--theme-color-info--active`       | `--si-sys-color-background-information-active` |
| `--theme-color-info--contrast`     | `--si-sys-color-text-on-information`           |
| `--theme-color-alarm-40`           | `--si-sys-color-background-danger-subtle`      |
| `--theme-color-alarm-10`           | `--si-sys-color-background-danger-subtle`      |
| `--theme-color-success-40`         | `--si-sys-color-background-success-subtle`     |
| `--theme-color-info-40`            | `--si-sys-color-background-information-subtle` |
| `--theme-color-neutral-40`         | `--si-sys-color-data-sequential-deep-blue-4`   |
| `--theme-color-critical-40`        | `--si-sys-color-background-critical-subtle`    |
| `--theme-color-warning--contrast`  | `--si-sys-color-text-on-warning`               |
| `--theme-color-warning-40`         | `--si-sys-color-background-caution-subtle`     |
| `--theme-color-warning-10`         | `--si-sys-color-background-caution-subtle`     |
| `--theme-color-warning--hover`     | `--si-sys-color-background-warning-hover`      |
| `--theme-color-warning--active`    | `--si-sys-color-background-warning-active`     |
| `--theme-color-critical--hover`    | `--si-sys-color-background-critical-hover`     |
| `--theme-color-critical--active`   | `--si-sys-color-background-critical-active`    |
| `--theme-color-critical--contrast` | `--si-sys-color-text-on-warning`               |
| `--theme-color-neutral--hover`     | `--si-sys-color-background-hover`              |
| `--theme-color-neutral--active`    | `--si-sys-color-background-selected`           |
| `--theme-color-neutral--contrast`  | `--si-sys-color-text-primary`                  |

#### Text and brand

| Legacy token                      | Replacement                     |
| --------------------------------- | ------------------------------- |
| `--theme-color-contrast-text`     | `--si-sys-color-text-primary`   |
| `--theme-color-std-text`          | `--si-sys-color-text-primary`   |
| `--theme-color-soft-text`         | `--si-sys-color-text-secondary` |
| `--theme-color-weak-text`         | `--si-sys-color-text-disabled`  |
| `--theme-color-inv-contrast-text` | `--si-sys-color-text-inverse`   |
| `--theme-color-inv-std-text`      | `--si-sys-color-text-inverse`   |
| `--theme-color-alarm-text`        | `--si-sys-color-text-danger`    |
| `--theme-color-inv-soft-text`     | `--si-sys-color-text-inverse`   |
| `--theme-color-inv-weak-text`     | `--si-sys-color-text-inverse`   |
| `--theme-color-warning-text`      | `--si-sys-color-text-warning`   |
| `--theme-color-logo`              | `--si-sys-color-effects-logo`   |
| `--theme-color-logo-login`        | `--si-sys-color-effects-logo`   |

#### Borders and effects

| Legacy token                 | Replacement                         |
| ---------------------------- | ----------------------------------- |
| `--theme-color-std-bdr`      | `--si-sys-color-border-2`           |
| `--theme-color-soft-bdr`     | `--si-sys-color-border-3`           |
| `--theme-color-weak-bdr`     | `--si-sys-color-border-4`           |
| `--theme-color-focus-bdr`    | `--si-sys-color-effects-focus`      |
| `--theme-color-contrast-bdr` | `--si-sys-color-border-1`           |
| `--theme-color-hard-bdr`     | `--si-sys-color-border-2`           |
| `--theme-color-alarm-bdr`    | `--si-sys-color-border-danger`      |
| `--theme-color-x-weak-bdr`   | `--si-sys-color-border-4`           |
| `--theme-color-warning-bdr`  | `--si-sys-color-background-warning` |
| `--theme-color-lightbox`     | `--si-sys-color-effects-backdrop`   |

#### Data visualization

| Legacy token              | Replacement                                   |
| ------------------------- | --------------------------------------------- |
| `--theme-chart-1`         | `--si-sys-color-data-categorical-2`           |
| `--theme-chart-2`         | `--si-sys-color-data-categorical-4`           |
| `--theme-chart-3`         | `--si-sys-color-data-categorical-1`           |
| `--theme-chart-4`         | `--si-sys-color-data-categorical-6`           |
| `--theme-chart-5`         | `--si-sys-color-data-categorical-17`          |
| `--theme-chart-6`         | `--si-sys-color-data-categorical-5`           |
| `--theme-chart-7`         | `--si-sys-color-data-categorical-13`          |
| `--theme-chart-8`         | `--si-sys-color-data-sequential-royal-blue-4` |
| `--theme-chart-9`         | `--si-sys-color-data-categorical-8`           |
| `--theme-chart-10`        | `--si-sys-color-data-categorical-7`           |
| `--theme-chart-11`        | `--si-sys-color-data-categorical-9`           |
| `--theme-chart-12`        | `--si-sys-color-data-categorical-11`          |
| `--theme-chart-13`        | `--si-sys-color-data-categorical-12`          |
| `--theme-chart-14`        | `--si-sys-color-data-sequential-orange-4`     |
| `--theme-chart-15`        | `--si-sys-color-data-sequential-orange-1`     |
| `--theme-chart-16`        | `--si-sys-color-data-sequential-sand-1`       |
| `--theme-chart-17`        | `--si-sys-color-data-categorical-16`          |
| `--theme-chart-grid-fill` | `--si-sys-color-background-1`                 |

### Conditional mappings

Choose the replacement from the column matching the use site:

- `background-color`, `background`, or a shape/surface `fill`: use
  **Background/fill**.
- `border-color`, `border`, or `outline`: use **Border/outline**.
- `color` on text or icons, or an icon SVG `fill`: use **Text/icon**.

For `fill`, icon fill is text/icon use; shape or surface fill is
background/fill use. An em dash means the source guide defines no replacement
for that context.

| Legacy token                    | Background/fill                           | Border/outline                        | Text/icon                           |
| ------------------------------- | ----------------------------------------- | ------------------------------------- | ----------------------------------- |
| `--theme-color-primary`         | `--si-sys-color-background-accent`        | `--si-sys-color-border-accent`        | `--si-sys-color-text-accent`        |
| `--theme-color-primary--hover`  | `--si-sys-color-background-accent-hover`  | `--si-sys-color-border-accent-hover`  | `--si-sys-color-text-accent-hover`  |
| `--theme-color-primary--active` | `--si-sys-color-background-accent-active` | `--si-sys-color-border-accent-active` | `--si-sys-color-text-accent-active` |
| `--theme-color-dynamic`         | `--si-sys-color-background-accent-hover`  | `--si-sys-color-border-accent-hover`  | `--si-sys-color-text-accent-hover`  |
| `--theme-color-component-9`     | `--si-sys-color-background-accent-hover`  | `--si-sys-color-border-accent-hover`  | `--si-sys-color-text-accent-hover`  |
| `--theme-color-alarm`           | `--si-sys-color-background-danger`        | `--si-sys-color-border-danger`        | `--si-sys-color-text-danger`        |
| `--theme-color-success`         | `--si-sys-color-background-success`       | `--si-sys-color-border-success`       | `--si-sys-color-text-success`       |
| `--theme-color-info`            | `--si-sys-color-background-information`   | `--si-sys-color-border-information`   | `--si-sys-color-text-information`   |
| `--theme-color-5`               | —                                         | `--si-sys-color-border-4`             | `--si-sys-color-text-disabled`      |
| `--theme-color-6`               | —                                         | `--si-sys-color-border-3`             | `--si-sys-color-text-disabled`      |
| `--theme-color-7`               | —                                         | `--si-sys-color-border-2`             | `--si-sys-color-text-secondary`     |
| `--theme-color-8`               | —                                         | `--si-sys-color-border-2`             | `--si-sys-color-text-secondary`     |
| `--theme-color-warning`         | `--si-sys-color-background-warning`       | `--si-sys-color-border-warning`       | `--si-sys-color-text-warning`       |
| `--theme-color-critical`        | `--si-sys-color-background-critical`      | `--si-sys-color-border-critical`      | `--si-sys-color-text-critical`      |
| `--theme-color-neutral`         | `--si-sys-color-background-neutral`       | `--si-sys-color-border-3`             | `--si-sys-color-text-secondary`     |

### Manual review

The following investigate entries need review at the component or usage site.
Where the suggested replacement is **No direct replacement**, preserve or
recreate the intended component-specific value rather than substituting a
generic token.

| Legacy token                           | Suggested replacement                                | Migration guidance                                                                                                |
| -------------------------------------- | ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| `--theme-color-primary--disabled`      | **No direct replacement**                            | Assess `--si-sys-color-border-3`, `--si-sys-color-border-4`, or `--si-sys-color-border-accent` for the component. |
| `--theme-color-dynamic--hover`         | `--si-sys-color-background-accent-hover`             | Verify the intended state and use in each component.                                                              |
| `--theme-color-dynamic--active`        | `--si-sys-color-background-accent-active`            | Verify the intended state and use in each component.                                                              |
| `--theme-color-dynamic-alt`            | `--si-sys-color-background-accent-hover`             | Verify the intended state and use in each component.                                                              |
| `--theme-color-dynamic-alt--hover`     | `--si-sys-color-background-accent-active`            | Verify the intended state and use in each component.                                                              |
| `--theme-color-dynamic-alt--active`    | `--si-sys-color-background-accent`                   | Verify the intended state and use in each component.                                                              |
| `--theme-color-component-2`            | **No direct replacement**                            | Component-level value; review or recreate the legacy `#FFFFFF 15%` value.                                         |
| `--theme-color-component-3`            | **No direct replacement**                            | Component-level value; review or recreate the legacy `#FFFFFF 30%` value.                                         |
| `--theme-color-component-4`            | **No direct replacement**                            | Component-level value; review or recreate the legacy `#FFFFFF 45%` value.                                         |
| `--theme-color-component-6`            | **No direct replacement**                            | Component-level value; review or recreate the legacy `#FFFFFF 75%` value.                                         |
| `--theme-color-component-7`            | **No direct replacement**                            | Component-level value; review or recreate the legacy `#16565C` value.                                             |
| `--theme-color-component-8`            | **No direct replacement**                            | Component-level value; review or recreate the legacy `#001F39` value.                                             |
| `--theme-color-component-8--hover`     | **No direct replacement**                            | Component-level value; review or recreate the legacy `#001F39` value.                                             |
| `--theme-color-component-9--hover`     | **No direct replacement**                            | Component-level value; review or recreate the legacy `#00E5AA` value.                                             |
| `--theme-color-component-9--active`    | **No direct replacement**                            | Component-level value; review or recreate the legacy `#00997F` value.                                             |
| `--theme-color-component-9--disabled`  | **No direct replacement**                            | Component-level value; review or recreate the legacy `#00FFB9 30%` value.                                         |
| `--theme-color-component-10`           | **No direct replacement**                            | Component-level value; review or recreate the legacy `#00FFB9 75%` value.                                         |
| `--theme-color-component-10--hover`    | **No direct replacement**                            | Component-level value; review or recreate the legacy `#00E5AA 75%` value.                                         |
| `--theme-color-component-10--active`   | **No direct replacement**                            | Component-level value; review or recreate the legacy `#00997F 90%` value.                                         |
| `--theme-color-component-10--disabled` | **No direct replacement**                            | Component-level value; review or recreate the legacy `#00FFB9 20%` value.                                         |
| `--theme-color-backdrop`               | **No direct replacement**                            | No replacement; preserve the legacy `#000028 85%` backdrop if required.                                           |
| `--theme-color-backdrop-3`             | **No direct replacement**                            | No replacement; preserve the legacy `#161632 85%` backdrop if required.                                           |
| `--theme-color-shadow-1`               | **No direct replacement**                            | Set the shadow color directly in shadow styles; legacy value is `#000000 60%`.                                    |
| `--theme-color-shadow-2`               | **No direct replacement**                            | Set the shadow color directly in shadow styles; legacy value is `#000000`.                                        |
| `--theme-color-gradient-effect-1`      | **No direct replacement**                            | Error Pages only; replaced by SDL. Legacy value is `#00FFB9`.                                                     |
| `--theme-color-gradient-effect-2`      | **No direct replacement**                            | Error Pages only; replaced by SDL. Legacy value is `#00CCCC`.                                                     |
| `--theme-chart-axes`                   | `--si-sys-color-border-3`                            | Verify the replacement on the component.                                                                          |
| `--theme-chart-grid-lines`             | `--si-sys-color-background-3`                        | Verify the replacement on the component.                                                                          |
| `--theme-chart-ticks`                  | `--si-sys-color-border-2`                            | Verify the replacement on the component.                                                                          |
| `--theme-chart-tooltip-fill`           | `--si-sys-color-background-3`                        | Verify the replacement on the component.                                                                          |
| `--theme-chart-tooltip-bdr`            | `--si-sys-color-border-2`                            | Verify the replacement on the component.                                                                          |
| `--theme-chart-1-40`                   | `--si-sys-color-data-sequential-turquoise-4`         | Verify; alternatively use the base color with alpha.                                                              |
| `--theme-chart-2-40`                   | `--si-sys-color-data-sequential-interactive-coral-4` | Verify; alternatively use the base color with alpha.                                                              |
| `--theme-chart-3-40`                   | `--si-sys-color-data-sequential-interactive-coral-4` | Verify; alternatively use the base color with alpha.                                                              |
| `--theme-chart-4-40`                   | `--si-sys-color-data-sequential-deep-blue-3`         | Verify; alternatively use the base color with alpha.                                                              |
| `--theme-chart-5-40`                   | `--si-sys-color-data-sequential-deep-blue-4`         | Verify; alternatively use the base color with alpha.                                                              |
| `--theme-chart-6-40`                   | `--si-sys-color-data-sequential-royal-blue-4`        | Verify; alternatively use the base color with alpha.                                                              |
| `--theme-chart-7-40`                   | `--si-sys-color-data-sequential-royal-blue-4`        | Verify; alternatively use the base color with alpha.                                                              |
| `--theme-chart-8-40`                   | `--si-sys-color-data-sequential-turquoise-4`         | Verify; alternatively use the base color with alpha.                                                              |
| `--theme-chart-9-40`                   | `--si-sys-color-data-categorical-17`                 | Verify; alternatively use the base color with alpha.                                                              |
| `--theme-chart-10-40`                  | `--si-sys-color-data-sequential-royal-blue-4`        | Verify; alternatively use the base color with alpha.                                                              |
| `--theme-chart-11-40`                  | `--si-sys-color-data-sequential-orchid-4`            | Verify; alternatively use the base color with alpha.                                                              |
| `--theme-chart-12-40`                  | `--si-sys-color-data-sequential-plum-4`              | Verify; alternatively use the base color with alpha.                                                              |
| `--theme-chart-13-40`                  | `--si-sys-color-data-sequential-plum-3`              | Verify; alternatively use the base color with alpha.                                                              |
| `--theme-chart-14-40`                  | `--si-sys-color-data-sequential-sand-4`              | Verify; alternatively use the base color with alpha.                                                              |
| `--theme-chart-15-40`                  | `--si-sys-color-data-sequential-sand-4`              | Verify; alternatively use the base color with alpha.                                                              |
| `--theme-chart-16-40`                  | `--si-sys-color-data-sequential-sand-3`              | Verify; alternatively use the base color with alpha.                                                              |
| `--theme-chart-17-40`                  | `--si-sys-color-data-sequential-sand-4`              | Verify; alternatively use the base color with alpha.                                                              |

## ECharts computed CSS properties

The `getComputedCSSProperty` helper exported by `@siemens/ix-echarts` no longer
adds the `--theme-` prefix to its argument. It now resolves the supplied CSS
custom-property name without modification.

Pass the complete SI Theme 6 system-token name, including the leading `--`:

```ts
// Before: resolves --theme-color-primary
getComputedCSSProperty('color-primary');

// After
getComputedCSSProperty('--si-sys-color-background-accent');
```

Replace every legacy short token name with its mapped system token. Select the
appropriate background, border, or text token for conditional mappings based on
how ECharts renders the value.

## Generated component token aliases

Standard IX foundation and theme CSS no longer emits generated
`--theme-<component>-*` custom properties such as
`--theme-btn-primary--background`. IX components and global style utilities now
declare scoped, purpose-based `--ix-*` properties where they are used.

Replace component overrides with the corresponding `--ix-*` property on the
component:

```css
/* Before */
[data-ix-theme] {
  --theme-btn-primary--background: var(--si-sys-color-background-accent);
}

/* After */
ix-button {
  --ix-button-primary--background: var(--si-sys-color-background-accent);
}
```

The generated aliases remain available as a temporary, opt-in Sass layer for
downstream styles that still reference them:

```scss
@use '@siemens/ix/scss/deprecated/components' as deprecated;

[data-ix-theme] {
  @include deprecated.setComponentVars;
}
```

This deprecated mixin restores the alias declarations for downstream CSS. It
does not reconnect those aliases to migrated IX components; component
customization must use the component's `--ix-*` properties.

## Global CSS and Sass entry points

The default `siemens-ix.css` stylesheet now contains the combined classic theme
and IX foundation. Foundation applies body typography, text color, background,
and document-wide scrollbar styles. It does not normalize native HTML elements,
reset the body margin, style native links or form controls, or provide utility
classes.

Use the canonical default import for self-contained IX components:

```css
@import '@siemens/ix/css/default.css';
```

The existing `@siemens/ix/dist/siemens-ix/siemens-ix.css` import path remains
available and resolves to the same default bundle. It includes the foundation
body and scrollbar defaults but not the previous reset, utility, or native form
styles.

Global application styles are available as explicit opt-ins:

| Import                           | Side effects                                                 |
| -------------------------------- | ------------------------------------------------------------ |
| `@siemens/ix/css/foundation.css` | Required variables, body presentation, and global scrollbars |
| `@siemens/ix/css/reset.css`      | Native element normalization and box sizing                  |
| `@siemens/ix/css/base.css`       | Theme font/color inheritance and body margin reset           |
| `@siemens/ix/css/scrollbar.css`  | Scoped scrollbars below `[data-ix-scrollbars]`               |
| `@siemens/ix/css/utilities.css`  | Typography, links, tables, groups, and shadows               |
| `@siemens/ix/css/globals.css`    | Reset, base, scrollbar, and utilities                        |
| `@siemens/ix/css/legacy.css`     | Previous native form and global style behavior               |

Import foundation before a separately selected theme:

```css
@import '@siemens/ix/css/foundation.css';
@import '@siemens/ix/css/theme/classic-light.css';
```

To retain the previous aggregate behavior while migrating, combine the new
default and legacy bundles:

```css
@import '@siemens/ix/css/default.css';
@import '@siemens/ix/css/legacy.css';
```

Applications that keep the existing distribution path can restore the same
behavior with:

```css
@import '@siemens/ix/dist/siemens-ix/siemens-ix.css';
@import '@siemens/ix/css/legacy.css';
```

Foundation styles enable scrollbars automatically. Add `disable-scrollbar` to
`body` to disable that behavior. The scoped scrollbar stylesheet can then
enable themed scrollbars for a selected subtree:

```html
<body class="disable-scrollbar">
  <main data-ix-scrollbars>
    <!-- Scrollable descendants use IX scrollbar styling. -->
  </main>
</body>
```

The package no longer exports every file below `@siemens/ix/scss/*`. Supported
Sass entry points are now explicit and mirror the CSS bundles:

```scss
@use '@siemens/ix/scss/default';
@use '@siemens/ix/scss/globals';
@use '@siemens/ix/scss/tokens/system' as sys;
```

Theme modules, retained public mixins, and
`@siemens/ix/scss/deprecated/components` remain explicitly exported. Replace
imports from internal `components`, `entries`, `legacy`, `misc`, and
`mixins/internal` folders with a supported public entry point or local styles.
The former `@siemens/ix/scss/misc/common-variables` path remains as a v6
compatibility export for generated custom themes; new Sass code should use
`@siemens/ix/scss/tokens/legacy`.

## Removed legacy Sass modules

The unused legacy Sass modules below have been removed:

- `@siemens/ix/scss/legacy/components/dropdown`
- `@siemens/ix/scss/legacy/components/modal`
- `@siemens/ix/scss/legacy/components/button-group`

These modules were not included in the compiled IX CSS. Remove direct imports
and use the corresponding IX components. For CSS class-based button groups, use
the supported `.ix-button-group` styles.

## Removed Sass mixins

The following unused or implementation-specific Sass modules have been
removed:

- `@siemens/ix/scss/mixins/field`
- `@siemens/ix/scss/mixins/reduce-motion`
- `@siemens/ix/scss/mixins/shadows`
- `@siemens/ix/scss/mixins/internal/dropdown-item`
- `@siemens/ix/scss/mixins/shadow-dom/focus-visible`
- `@siemens/ix/scss/mixins/shadow-dom/host-disabled`
- `@siemens/ix/scss/mixins/shadow-dom/hover`

The unused `hover.host-focus`, `hover.host-focus-visible`, `input.label`, and
`input.element-form` mixins have also been removed.

Use `@siemens/ix/scss/mixins/hover` for retained interaction helpers. Replace
`shadows.box-shadow($level)` with
`box-shadow: var(--si-sys-color-effects-shadow-#{$level})`; the variable supplies the
complete shadow declaration value. Reduced-motion timing overrides remain part
of the standard IX foundation CSS and no longer require a separate mixin.

## Icon and component color properties

`@siemens/ix-icons` now passes its `color` value directly to `var(...)`.
Properties that forward a color to an icon, and `ix-event-list-item.itemColor`,
therefore require the complete CSS custom-property name:

```html
<!-- Before -->
<ix-icon color="si-sys-text-primary"></ix-icon>
<ix-icon-button icon-color="color-primary"></ix-icon-button>
<ix-event-list-item item-color="color-alarm"></ix-event-list-item>

<!-- After -->
<ix-icon color="--si-sys-color-text-primary"></ix-icon>
<ix-icon-button icon-color="--si-sys-color-text-accent"></ix-icon-button>
<ix-event-list-item item-color="--si-sys-color-background-danger"></ix-event-list-item>
```

The exported `NotificationColor` string literals now include the leading `--`.

## Content header text wraps by default

`ix-content-header` titles and subtitles now wrap when horizontal space is limited. Previously, they remained on a single line and used an ellipsis for overflowing text.

To preserve the previous single-line behavior, set `text-overflow="ellipsis"`:

```html
<ix-content-header header-title="Content title" header-subtitle="Supporting context" text-overflow="ellipsis"></ix-content-header>
```

Ellipsis visually truncates the text without adding a tooltip. Keep the default wrapping behavior when the complete text must remain visible.

### Validation checklist

1. Every `ix-content-header` that must remain single-line uses `text-overflow="ellipsis"`.

## Hydration class changed to attribute

The hydration class creates issues with frameworks such as React when the DOM is updated via the framework, causing bugs such as [IX component invisibility on className update](https://github.com/siemens/ix/issues/2668).

We have migrated to using the `hydrated` attribute instead, which is also fully supported by all frameworks but removes the potential for class clashing or overwriting. 

Components are now marked as hydrated with a `hydrated` attribute instead of a `hydrated` CSS class

### Migration steps

Update any code that relies on the class marker:

- CSS/SCSS selectors: replace `.hydrated` with `[hydrated]` (e.g. `ix-button.hydrated` → `ix-button[hydrated]`).
- JavaScript/TypeScript checks: replace `element.classList.contains('hydrated')` with `element.hasAttribute('hydrated')`.
- Test assertions: replace class-based matchers (e.g. Playwright `toHaveClass(/hydrated/)`) with attribute-based ones (`toHaveAttribute('hydrated')`).

## Date Time Picker - locale support

Locale support was extended to date-time values and headers, so the return value strings will now be localised.

The `from` and `to` fields on `DateChangeEvent` (emitted by `ix-date-picker`, `ix-date-dropdown`) and `from`, `to`, and `time` on `DateTimeSelectEvent` (emitted by `ix-datetime-picker`) were previously always formatted using the `en-US` locale, regardless of the `locale` prop. They now reflect the locale set on the component.

If your code reads these fields and expects locale-neutral strings, migrate to the new `isoFrom`, `isoTo`, and `isoTime` fields, which always contain ISO 8601 values independent of locale.

**Note**: If you pass a `format` string, ensure to use localized formats ([see supported formats table](https://moment.github.io/luxon/#/formatting?id=table-of-tokens)) tokens in order to ensure locale-specific rendering of the date-time values.

**New fields added to event types:**

- `DateChangeEvent`: `isoFrom?: string`, `isoTo?: string`
- `DateRangeChangeEvent`: `isoFrom?: string`, `isoTo?: string`
- `DateTimeSelectEvent`: `isoFrom?: string`, `isoTo?: string`, `isoTime?: string`

  **New props:**

- `ix-time-picker` and `ix-time-input`: `locale`, `i18nAm`, and `i18nPm`
- `ix-datetime-picker`: `i18nAm`, `i18nPm`, `i18nHourColumnHeader`, `i18nMinuteColumnHeader`, `i18nSecondColumnHeader`, and `i18nMillisecondColumnHeader`

**New methods:**

- `ix-time-picker`: `getCurrentIsoTime(): Promise<string | undefined>` — returns the current time as an ISO 8601 string, independent of the `locale` prop

**Updated `ix-date-input` locale behavior:**
The `locale` prop now explicitly affects input parsing and the values emitted via `valueChange`. Previous documentation incorrectly stated that locale does not affect returned values.

## Default icon size is 20

Omitted icon `size` on **ix-icon-button**, **ix-icon-toggle-button**, and nested icons that inherit that default now renders a **20px** glyph instead of **24px**. The control host for size `20` and `24` remains **32×32**. Size `20` is also available as an explicit public value.

Most composed components follow the same omit→20 default. A smaller set of chrome icons stay on an intentional larger or smaller size (pinned in code). Treat **Figma** as the source of truth for which slots are 16 / 20 / 24 / 32; do not use this document as a size catalog.

**ix-checkbox** and **ix-radio** control glyphs also move to **20** (hit area stays 24×24). Legacy `.ix-form-control` checkboxes align to the same outer size.

```html
<!-- Before (omit → 24px glyph) -->
<ix-icon-button icon="rocket"></ix-icon-button>

<!-- After (omit → 20px glyph) -->
<ix-icon-button icon="rocket"></ix-icon-button>

<!-- Keep the previous 24px glyph -->
<ix-icon-button icon="rocket" size="24"></ix-icon-button>
```

### Migration checklist

1. Visually review screens that relied on omitted icon `size` for a 24px glyph.
2. Set `size="24"` (or the previous explicit size) wherever the previous glyph must remain.
3. Prefer omitting `size` when the new 20px default matches the design; do not hardcode `size="20"` in product code.
4. For intended sizes on composed chrome (headers, menu, tabs, etc.), follow the current Figma specs rather than assuming every icon moved to 20.

## `ix-spinner` size scale

Spinner `size` tokens are renamed and remapped as follows.

| Prev prop | Was | New prop | Now |
| --- | --- | --- | --- |
| `xx-small` | 12px | `xxs` | 12px |
| `x-small` | 20px | `xs` | **16px** |
| `small` | 24px | `sm` | **20px** |
| `medium` | 32px (default) | `md` | 32px (default) |
| `large` | 96px | `xxl` | 96px |

There is no 24px spinner size. To keep a similar visual size, migrate props: `xx-small` → `xxs`, `x-small` (20) → `sm`, `small` (24) → `sm` or `md`, `medium` → `md`, `large` → `xxl`.

```html
<!-- Before -->
<ix-spinner size="xx-small"></ix-spinner>
<ix-spinner size="x-small"></ix-spinner>
<ix-spinner size="small"></ix-spinner>
<ix-spinner size="medium"></ix-spinner>
<ix-spinner size="large"></ix-spinner>

<!-- After -->
<ix-spinner size="xxs"></ix-spinner>
<ix-spinner size="sm"></ix-spinner>
<ix-spinner size="sm"></ix-spinner>
<ix-spinner size="md"></ix-spinner>
<!-- or omit size for the default -->
<ix-spinner></ix-spinner>
<ix-spinner size="xxl"></ix-spinner>
```

### Migration checklist

1. Rename tokens: `xx-small` → `xxs`, `medium` → `md`, `large` → `xxl`.
2. If you relied on previous pixel sizes: `x-small` (20) → `sm`; `small` (24) → `sm` (20) or `md` (32).
3. Use `xs` (16) / `sm` (20) where those sizes are intended.
4. Prefer omitting `size` when the default `md` (32px) is correct.

## `ix-progress-indicator` size scale

Circular size token names are unchanged. **`sm` is remapped from 24px to 20px.**

| Prev prop | Was | New prop | Now |
| --- | --- | --- | --- |
| `xs` | 16px | `xs` | 16px |
| `sm` | 24px | `sm` | **20px** |
| `md` | 32px (default) | `md` | 32px (default) |
| `lg` | 48px | `lg` | 48px |
| `xl` | 64px | `xl` | 64px |

There is no 24px circular size. If you need a larger visual than 20px, use `md` (32px) or omit `size` for the default.

```html
<!-- Before / After: same prop name; sm is now 20px instead of 24px -->
<ix-progress-indicator type="circular" size="sm" value="75"></ix-progress-indicator>
```

### Migration checklist

1. Audit circular `size="sm"` usages for the 24→20 visual change.
2. Prefer `md` (or omit) when 32px is acceptable; keep `sm` only when 20px is intended.

