{{^embedded}}
import ApiFunction from '@site/src/components/ApiTable/ApiFunction';
import { DeprecatedTag, SinceTag } from '@site/src/components/UI/Tags';
import ReactMarkdown from 'react-markdown';
{{/embedded}}

{{#functions}}
<ApiFunction
  name="{{name}}"
  returnType="{{returnType}}"
  {{#hasParameters}}
  parameters={[
    {{#parameters}}
    {
      name: "{{name}}",
      type: "{{type}}",
      {{#optional}}optional: true,{{/optional}}
      {{#default}}defaultValue: "{{default}}",{{/default}}
    },
    {{/parameters}}
  ]}
  {{/hasParameters}}
  {{#singleFramework}}
  singleFramework="{{singleFramework}}"
  {{/singleFramework}}
>
  {{#docsTags}}
  {{{rTag}}}
  {{/docsTags}}

  <ReactMarkdown>{ {{{comment}}} }</ReactMarkdown>
</ApiFunction>
{{/functions}}
