import {ApiTableSinceTag, ApiTableDeprecatedTag} from '@site/src/components/ApiTableTag';
import FrameworkSelection from '@site/src/components/UI/FrameworkSelection';

{{#props}}
<div className="api-table container mx-auto mb-8">
  <div className="bg-[transparent] rounded-lg overflow-hidden border-solid border-[1px] border-[var(--theme-color-soft-bdr)]">
    <div className="flex bg-[var(--theme-color-2)] text-[var(--theme-color-std-text)] p-4 border-solid border-0 border-b border-[var(--theme-color-soft-bdr)]">
      <div className="flex items-center font-bold">{{ name }}</div>
      <div className="flex items-center ml-auto gap-2">
      {{#docsTags}}
        {{{ rTag }}}
      {{/docsTags}}
        <FrameworkSelection />
      </div>
    </div>

    <div className="grid grid-cols-[minmax(100px,20%)_1fr] gap-2 border-solid border-0 border-b border-[var(--theme-color-soft-bdr)]">
      <div className="px-8 py-4 font-bold w-auto border-solid border-0 border-r border-[var(--theme-color-soft-bdr)]">
        Description
      </div>
      <div className="p-4 w-auto">{ `{{ docs }}` }</div>
    </div>

    <div className="grid grid-cols-[minmax(100px,20%)_1fr] gap-2 border-solid border-0 border-b border-[var(--theme-color-soft-bdr)]">
      <div className="px-8 py-4 font-bold w-auto border-solid border-0 border-r border-[var(--theme-color-soft-bdr)]">
        Attribute
      </div>
      <div className="p-4 w-auto"><code className="p-1">{{ attr }}</code></div>
    </div>

    <div className="grid grid-cols-[minmax(100px,20%)_1fr] gap-2 border-solid border-0 border-b border-[var(--theme-color-soft-bdr)]">
      <div className="px-8 py-4 font-bold w-auto border-solid border-0 border-r border-[var(--theme-color-soft-bdr)]">
        Type
      </div>
      <div className="p-4 w-auto"><code className="p-1">{ `{{{ type }}}` }</code></div>
    </div>

    <div className="grid grid-cols-[minmax(100px,20%)_1fr] gap-2">
      <div className="px-8 py-4 font-bold w-auto border-solid border-0 border-r border-[var(--theme-color-soft-bdr)]">
        Default
      </div>
      <div className="p-4 w-auto"><code className="p-1">{ `{{{ default }}}` }</code></div>
    </div>

  </div>
</div>
{{/props}}
