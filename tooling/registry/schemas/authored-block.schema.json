{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "IX Authored Block Definition",
  "description": "Repository-only schema for Siemens IX block definitions",
  "type": "object",
  "required": ["name", "description", "keywords", "preview", "variants"],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "name": {
      "type": "string",
      "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
    },
    "description": {
      "type": "string",
      "minLength": 1
    },
    "keywords": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1
      },
      "uniqueItems": true
    },
    "preview": {
      "type": "string",
      "format": "uri-reference"
    },
    "variants": {
      "type": "object",
      "minProperties": 1,
      "properties": {
        "react": {
          "$ref": "#/definitions/variant"
        },
        "angular": {
          "$ref": "#/definitions/variant"
        },
        "vue": {
          "$ref": "#/definitions/variant"
        }
      },
      "additionalProperties": {
        "$ref": "#/definitions/variant"
      }
    }
  },
  "additionalProperties": false,
  "definitions": {
    "safeRelativePath": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(?![A-Za-z]:)(?!.*[:?#\\\\%])(?!(?:.*/)?\\.{1,2}(?:/|$))[^/]+(?:/[^/]+)*$"
    },
    "variant": {
      "type": "object",
      "required": ["files"],
      "properties": {
        "files": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "required": ["sourcePath"],
            "properties": {
              "sourcePath": {
                "$ref": "#/definitions/safeRelativePath"
              }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    }
  }
}
